#pragma once

#include <algorithm> // find
#include <string> // rand
#include <vector>

using namespace std;

class gen_utils {
	public:
	static double obtain_double();
	static bool contains(vector<int> v, int e);
};
